---
import Layout from '../layouts/Layout.astro';
import ChristmasOrderModal from '../components/forms/ChristmasOrderModal.jsx';
---

<Layout title="Offerta Speciale Natale" breadcrumbs={[{ text: 'Home', href: '/' }, { text: 'Offerta Speciale Natale', href: '/offerte-natale' }]}>
  <main>
    <!-- Hero Section -->
    <section class="hero">
      <img src='/fotolavoretti_logo_natale.png' alt="Logo Natalizio Fotolavoretti">
      <h1>Offerta Speciale Natale!</h1>
      <p class="subtitle">Crea le tue cartoline di Natale digitali, animate e personalizzate</p>
      <p class="deadline">Disponibile fino al 22 Dicembre!</p>
    </section>

    <!-- Intro Section -->
    <section class="intro container">
      <p>
        Trasformo i disegni dei tuoi figli in auguri natalizi animati, personalizzati e pronti da condividere. 
        Ogni cartolina nasce dal disegno originale del bambino e include animazione, musica e un messaggio speciale per le feste. 
        Ricevi il video in formato ottimizzato, pronto da condividere sui social.
      </p>
    </section>

    <!-- Options Section -->
    <section class="options-container">
      <h2 class="main-title">Scegli la tua cartolina di Natale</h2>
      <div class="cards-grid">
        <!-- Opzione 1 -->
        <div class="card" data-card-name="Opzione 1">
          <div class="card-header">
          <h3 class="package-public">DEDICATA AL BAMBINO</h3>
            <h3 class="package-name">Standar</h3>
          </div>
          <div class="collapsible-content">
            <div class="card-body">
              <video class="video-placeholder" src="/videos/card_standar.mp4" autoplay loop playsinline controls muted></video>
              <p class="description">Un personaggio creato dal tuo piccolo prende vita e lo saluta con un messaggio standard: lo ringrazia per averlo disegnato e gli augura un felice Natale.</p>
              <p class="description">*Durata totale: 15–20 secondi (contenuto principale < 15 s).</p> 
              <p class="description">*Formato verticale 9:16 • HD • MP4 </p>
              <p class="description">*Messaggio standard con musica • Personalizzato con il nome del bambino</p>

             
              <div class="features-section">
                <p class="section-label">Include:</p>
                <ul>  
                  <li><span class="check-icon">✔</span>Video verticale animato (la card)</li>
                  <li><span class="check-icon">✔</span>Immagine social dal disegno del bambino</li>
                  <li><span class="check-icon">✔</span>Clip art in regalo</li>
                </ul>
              </div>
              <div class="target-section">
                <p class="section-label">Cosa mi serve:</p>
                <ul>
                  <li><span class="check-icon">👉</span>Foto del disegno del personaggio</li>
                  <li><span class="check-icon">👉</span>Nome del bambino</li>
                </ul>
              </div>
            </div>
            <div class="card-footer">
              <button class="cta-button" data-option="1">Prenota card standar (€15)</button>
            </div>
          </div>
        </div>

        <!-- Opzione 2 -->
        <div class="card highlighted" data-card-name="Opzione 2">
          <div class="card-header">
           <h3 class="package-public">DEDICATA AL BAMBINO</h3>
            <h3 class="package-name">Speciale</h3>
          </div>
          <div class="collapsible-content">
            <div class="card-body">
              <video class="video-placeholder" src="/videos/card_speciale.mp4" autoplay loop playsinline controls muted></video>
              <p class="description">Una versione ancora più speciale: il personaggio prende vita con un messaggio scelto da te, pensato su misura per il tuo bambino.
  Un augurio animato unico, più lungo e più personale, da conservare e condividere.</p>
              <p class="description">*Durata totale: 30–50 secondi (contenuto principale < 30 s).</p>
              <p class="description">*Video consegnato in formato verticale (9:16) e orizzontale (16:9) • HD • MP4 </p>
              <p class="description">*Messaggio e musica a scelta del cliente • Inclusa la menzione del nome del bambino</p>

              <div class="features-section">
                <p class="section-label">Include:</p>
                 <li><span class="check-icon">✔</span> Card animata in doppio formato: verticale (9:16) e orizzontale (16:9)</li>                 
                  <li><span class="check-icon">✔</span>Immagine social dal disegno del bambino</li>
                  <li><span class="check-icon">✔</span>Clip art in regalo</li>
              </div>
              <div class="target-section">
                <p class="section-label">Cosa mi serve:</p>
                <ul>
                  <li><span class="check-icon">👉</span>Foto del disegno del personaggio</li>
                  <li><span class="check-icon">👉</span>Nome del bambino</li>
                    <li><span class="check-icon">👉</span> Testo per il messaggio vocale del personaggio</li>
                    <li><span class="check-icon">👉</span> Musica a scelta</li>
                </ul>
              </div>
            </div>
            <div class="card-footer">
              <button class="cta-button" data-option="2">Prenota card Speciale (€25)</button>
            </div>
          </div>
        </div>
         

        <!-- Opzione 3 -->
        <div class="card" data-card-name="Opzione 3">
          <div class="card-header">
          <h3 class="package-public">DEDICATA ALLA FAMIGLIA</h3>
            <h3 class="package-name">Famiglia</h3>
          </div>
          <div class="collapsible-content">
            <div class="card-body">
              <video class="video-placeholder" src="/videos/card_famiglia.mp4" autoplay loop muted playsinline controls></video>
              <p class="description">Un disegno natalizio si anima con effetti magici, accompagnato dall’audio di auguri che ci invierai.</p>
               <p class="description">*Durata totale: 30–60 secondi (secondo il tempo del tuo saluto in audio).</p>
              <p class="description">*Video consegnato in formato orizzontale (16:9) • HD • MP4 </p>
              <p class="description">*Messaggio, testo e musica a scelta del cliente</p>
              <div class="features-section">
                <p class="section-label">Include:</p>
                <ul>
                  <li><span class="check-icon">✔</span> Card animata in formato orizzontale (16:9)</li>                 
                  <li><span class="check-icon">✔</span>Immagine social dal disegno del bambino</li>
                  <li><span class="check-icon">✔</span>Clip art in regalo</li>
                </ul>
              </div>
              <div class="target-section">
                <p class="section-label">Cosa mi serve:</p>
                <ul>
                  <li><span class="check-icon">👉</span>Foto del disegno natalizio</li>
                  <li><span class="check-icon">👉</span>Audio con il tuo messaggio di auguri</li>
                  <li><span class="check-icon">👉</span>Testo da inserire nel video (opzionale)</li>
                  <li><span class="check-icon">👉</span>Musica a scelta da inserire nel video (opzionale)</li>
                </ul>
              </div>
            </div>
            <div class="card-footer">
              <button class="cta-button" data-option="3">Prenota card Famiglia (€20)</button>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Instructions Section -->
    <section class="instructions container">
      <h2>Come funziona</h2>
      <div class="instructions-grid">
        <div class="instruction-step">
          <h4>1. Scegli la tua cartolina</h4>
          <p>Seleziona la cartolina che preferisci e compila il modulo d’ordine. Ti contatteró a breve.</p>
        </div>
        <div class="instruction-step">
          <h4>2. Inviaci foto e audio e conferma il pagamento</h4>
          <p>Ti scriveremo su Instagram o WhatsApp, come preferisci. Raccoglieremo i materiali necessari e ti invieremo il link per completare il pagamento.</p>
        </div>
        <div class="instruction-step">
          <h4>3. Ricevi la tua cartolina di Natale</h4>
          <p>Entro i tempi indicati riceverai il lavoro finito, pronto da conservare e condividere.</p>
        </div>
      </div>
    </section>

    <ChristmasOrderModal client:load />

  </main>
</Layout>

<script>
  // Modal script
  const modalButtons = document.querySelectorAll('.cta-button[data-option]');
  modalButtons.forEach(button => {
    button.addEventListener('click', (e) => {
      e.preventDefault();
      e.stopPropagation(); // Prevent click from bubbling up to the card header
      const selectedOption = button.getAttribute('data-option');
      const event = new CustomEvent('open-christmas-modal', { detail: { option: selectedOption } });
      document.dispatchEvent(event);
    });
  });

  // Accordion script
  const allCards = document.querySelectorAll('.card');
  allCards.forEach(card => {
    const header = card.querySelector('.card-header');
    header.addEventListener('click', () => {
      // This only runs on mobile where the header is clickable
      if (window.innerWidth < 1024) {
        const isOpen = card.classList.contains('open');
        
        // Close all cards
        allCards.forEach(c => c.classList.remove('open'));

        // If the clicked card was not open, open it
        if (!isOpen) {
          card.classList.add('open');
        }
      }
    });
  });
</script>

<style>
  /* General Container */
  .container {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 1rem;
  }

  /* Hero Section (Existing Styles) */
  .hero { text-align: center; padding: 2rem 0.5rem; color: black; }
  .hero img { object-fit: contain; height:30rem; width: 30rem; }
  .hero h1 { font-size: var(--font-size-xl); color: blue; margin: 0; }
  .hero .subtitle { font-size: var(--font-size-md); margin: 0.5rem 0; }
  .hero .deadline { font-size: var(--font-size-base); font-weight: var(--fw-bold); color: var(--color-primary); }

  /* Intro Section (Existing Styles) */
  .intro { widht: 50%; margin: auto; padding-top: 0; text-align: center; font-size: var(--font-size-lg); line-height: 1.5; }

  /* New Options Section Styles */
  .options-container {
    width: 90vw;
    max-width: 1100px;
    margin: auto;
    padding: 3rem 1rem;
    text-align: center;
  }
  .main-title {
    font-size: var(--font-size-lg);
    font-weight: var(--fw-bold);
    color: var(--color-text);
    margin-bottom: 2rem;
    border-bottom: 1px solid var(--color-accent);
    padding-bottom: 0.5rem;
    display: inline-block;
  }
  .cards-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    align-items: stretch;
  }
  .card {
    background: var(--color-background);
    border-radius: 12px;
    box-shadow: 0 8px 25px rgba(0,0,0,0.04);
    display: flex;
    flex-direction: column;
    border: 1px solid var(--color-accent);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    overflow: hidden;
  }
  .card:hover {
    transform: translateY(-5px);
    box-shadow: 0 12px 35px rgba(0,0,0,0.06);
  }
  .card.highlighted {
    border: 2px solid var(--color-primary);
  }
  .card.highlighted .package-name {
    color: var(--color-primary);
  }
  .card-header {
    padding: 1.5rem 1.5rem 0.5rem 1.5rem;
    border-bottom: 1px solid var(--color-accent);
    text-align: center;
  }
  .package-name {
    font-size: var(--font-size-md);
    margin: 0 0 10px 0;
    color: var(--color-text);
    font-weight: var(--fw-semibold);
  }
  .collapsible-content {
    display: flex;
    flex-direction: column;
    flex-grow: 1;
  }
  .card-body {
    padding: 1.5rem;
    flex-grow: 1;
    text-align: left;
  }
  .video-placeholder {
    max-width: 100%;
    max-height: 250px;
    width: auto;
    margin: 0 auto 1.5rem auto;
    display: block;
    border-radius: 6px;
    border: 1px solid var(--color-accent);
  }
  .description {
    font-size: var(--font-size-sm);
    color: var(--color-secondary);
    font-style: italic;
    margin-bottom: 1.5rem;
  }
  .section-label {
    font-weight: var(--fw-semibold);
    color: var(--color-text);
    font-size: var(--font-size-sm);
    text-transform: uppercase;
    margin-bottom: 1rem;
    margin-top: 0;
  }
  .features-section ul, .target-section ul {
    list-style: none;
    padding: 0;
    margin: 0 0 1.5rem 0;
  }
  .features-section li, .target-section li {
    margin-bottom: 0.8rem;
    font-size: var(--font-size-sm);
    color: var(--color-text);
    line-height: 1.5;
    display: flex;
    align-items: flex-start;
  }
  .check-icon {
    color: var(--color-secondary);
    margin-right: 10px;
    flex-shrink: 0;
  }
  .card.highlighted .check-icon {
    color: var(--color-primary);
  }
  .card-footer {
    padding: 1.5rem;
    margin-top: auto;
  }
  .cta-button {
    width: 100%;
    padding: 1rem;
    border: none;
    border-radius: 6px;
    background-color: var(--color-primary);
    color: white;
    font-weight: var(--fw-semibold);
    font-size: var(--font-size-base);
    cursor: pointer;
    transition: background-color 0.3s ease;
  }
  .cta-button:hover {
    background-color: var(--color-secondary);
  }

  /* Instructions Section (Existing Styles) */
  .instructions { padding: 4rem 2rem; }
  .instructions h2 { text-align: center; margin-bottom: 2rem; font-size: var(--font-size-lg); }
  .instructions-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 2rem; text-align: center; }
  .instruction-step h4 { font-size: var(--font-size-md); color: var(--color-secondary); }

  /* Mobile-specific accordion styles */
  @media (max-width: 1023px) {
    .options-container {
      width: 100%;
      padding: 2rem 1rem;
    }
    .cards-grid {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }
    .card-header {
      cursor: pointer;
      position: relative;
    }
    .card-header::after {
      content: '+';
      position: absolute;
      right: 1.5rem;
      top: 50%;
      transform: translateY(-50%);
      font-size: 2rem;
      color: var(--color-secondary);
      font-weight: var(--fw-light);
    }
    .card.open .card-header::after {
      content: '−';
    }
    .collapsible-content {
      display: block;
      flex-grow: 0;
      max-height: 0;
      overflow: hidden;
      transition: max-height 0.4s ease-out;
    }
    .card.open .collapsible-content {
      max-height: 1000px; /* Large enough to fit content */
      transition: max-height 0.5s ease-in;
    }
  }
</style>
